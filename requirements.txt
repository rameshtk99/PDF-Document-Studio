# Runtime dependencies, pinned to versions tested against this codebase
# and against the PyInstaller build in requirements-build.txt. Do not
# widen these to unbounded ("numpy", "reportlab") specifiers for a
# production build -- an arbitrary newer/older version picked up at build
# time is exactly what caused the numpy._core packaging failure (see
# requirements-build.txt for the matching PyInstaller-side pin).
numpy==2.4.6
pypdf==6.7.0
reportlab==4.4.10
PyMuPDF==1.28.2
pdf2image==1.17.0
Pillow==12.1.1
